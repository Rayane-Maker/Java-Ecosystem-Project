/**
 * This interface gather all the methods for player actions.
 * Such as the ability to observe and display the animal voices
 * on the waterlilies in front of the player
 */
public interface Player {
    public String[][] observeForward(Row[] pond);
}